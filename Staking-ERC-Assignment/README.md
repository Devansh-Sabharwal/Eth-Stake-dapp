This template uses the latest version of the forge-std library.
The default Foundry forge-template from foundry-rs ships with an outdated forge-std,
which can cause issues when using console.log with uint256 and other types.

To initialize a new Foundry project using this updated template, run:

forge init -t Devansh-Sabharwal/forge-template
